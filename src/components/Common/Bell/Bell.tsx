import React, { FC } from 'react';

import {Border} from '../../../styledHelpers/Components';

export const Bell: FC = () => {
    return(
        <Border>
            Tutaj będą pwiiadomienia
        </Border>
    )
}